import Site from "@/components/Site";

export const metadata = { title: "Werld Cap — Clean-jank hybrid (preview)" };

export default function Page() {
  return <Site theme="cleanjank" showThemeSwitcher />;
}
