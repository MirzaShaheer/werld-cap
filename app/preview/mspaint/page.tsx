import Site from "@/components/Site";

export const metadata = { title: "Werld Cap — MS-Paint shitpost (preview)" };

export default function Page() {
  return <Site theme="mspaint" showThemeSwitcher />;
}
